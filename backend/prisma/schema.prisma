datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Usuario {
  id             Int     @id @default(autoincrement())
  nombreCompleto String
  usuario        String  @unique
  contrasena     String
  rol_id         Int
  rol            Rol     @relation(fields: [rol_id], references: [id])
}

model Rol {
  id          Int            @id @default(autoincrement())
  nombre      String
  permisos    RolPermiso[]
  usuarios    Usuario[]
}

model Permiso {
  id       Int            @id @default(autoincrement())
  nombre   String
  ruta     String
  roles    RolPermiso[]
}

model RolPermiso {
  rol_id     Int
  permiso_id Int
  rol        Rol       @relation(fields: [rol_id], references: [id])
  permiso    Permiso   @relation(fields: [permiso_id], references: [id])
  @@id([rol_id, permiso_id])
}

// ... Define aquí el resto de tus modelos: Escuela, Ciclo, Grupo, Docente, Laboratorio, Reserva, etc.
